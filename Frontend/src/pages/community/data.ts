// Community feed data is now served live from the backend
// (GET /api/community/reports). See `community` in src/lib/endpoints.ts and the
// `CommunityReport` type in src/lib/types.ts.
//
// This module is intentionally left as a placeholder so the import path remains
// stable; all card data is fetched at runtime in CommunityPage.tsx.

export {};
